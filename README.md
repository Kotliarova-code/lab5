
📦 Как запустить

Требования:
- Microsoft Visual Studio 2019 или выше
- .NET Framework 4.7.2 или выше
- SQL Server LocalDB (входит в состав Visual Studio)

 Установка:
1. Скачать репозиторий
2. Открыть файл решения `Form_OOP_NoDB.sln` в Visual Studio
3. Восстановить NuGet пакеты (если требуется)
4. Скомпилировать и запустить проект (`Ctrl + F5`)

 Первый запуск:
1. При первом запуске автоматически создается база данных `HotelManagementDB`
2. Создается таблица `Guests` со всеми необходимыми полями
3. Для тестирования можно использовать функцию **"Создать тестовые данные"**

🗄️ Настройка базы данных

Подключение к базе:
- **Сервер:** `(localdb)\MSSQLLocalDB`
- **База данных:** `HotelManagementDB`
- **Аутентификация:** Windows Authentication

 Просмотр данных:
1. Открыть **SQL Server Object Explorer** в Visual Studio
2. Найти `(localdb)\MSSQLLocalDB → Databases → HotelManagementDB`
3. Развернуть `Tables → dbo.Guests`
4. Кликнуть ПКМ → **View Data**

 Резервное копирование:
- Используйте функцию **"Создать резервную копию"** в меню программы
- Файлы резервных копий сохраняются с расширением `.bak`

🔧 Основные функции программы

 Управление гостями:
1. **Добавить гостя** - открывает форму с пустыми полями
2. **Редактировать гостя** - открывает форму с заполненными данными выбранного гостя
3. **Удалить гостя** - удаляет выбранную запись из базы данных

 Работа с файлами:
1. **Экспорт в CSV** - сохраняет все данные в CSV файл
2. **Импорт из CSV** - загружает данные из CSV файла в базу
3. **Создать резервную копию** - создает полную резервную копию базы данных
4. **Восстановить из копии** - восстанавливает базу данных из резервной копии

 Сервисные функции:
1. **Тест подключения** - проверяет соединение с базой данных
2. **Очистить базу** - удаляет все записи (с подтверждением)
3. **Создать тестовые данные** - заполняет базу тестовыми данными

 📊 Форматы файлов

- **.dat** - бинарный формат сериализации (локальное хранение)
- **.csv** - текстовый формат с разделителями (экспорт/импорт)
- **.bak** - резервные копии базы данных SQL Server

 ⚠️ Примечания

- Программа требует наличия SQL Server LocalDB
- При отсутствии LocalDB можно установить его через [установщик](https://learn.microsoft.com/en-us/sql/database-engine/configure-windows/sql-server-express-localdb)
- Все изменения в базе данных сохраняются автоматически
- Рекомендуется регулярно создавать резервные копии

 📝 Лицензия

Проект разработан в учебных целях. Свободное использование и модификация разрешены.
